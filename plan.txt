load current balance data (long flag, cash balance)

poll coinbase for btc price every minute
save btc price in queue data struct
calculate macd and signal
check order status
if positive macd crossover and holding cash --> put in limit order for current ask with all cash.
    - if filled, log purchase price, amount, time
    - if macd goes below signal before order is filled, cancel order
if negative macd crossover --> check if btc holdings are nonzero --> if holding btc and current bid is greater than cost basis + fees, sell at bid. Else, do nothing
    - if filled, log sell price, amount, profit, time
    - if macd goes above signal before order is filled, cancel order
